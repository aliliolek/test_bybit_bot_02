import React from 'react';
import { Box, Typography } from '@mui/material';

const Ads: React.FC = () => {
  return (
    <Box p={2}>
      <Typography variant="h6">Ads</Typography>
    </Box>
  );
};

export default Ads;
